<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
    <RootNamespace>Seqeron.Genomics.MolTools</RootNamespace>
    <AssemblyName>Seqeron.Genomics.MolTools</AssemblyName>
    
    <PackageId>Seqeron.Genomics.MolTools</PackageId>
    <Version>1.0.0</Version>
    <Authors>Seqeron</Authors>
    <Description>Molecular biology tools: primer design, probe design, CRISPR design, codon optimization.</Description>
    <PackageTags>bioinformatics;genomics;primer;crispr;codon-optimization</PackageTags>
  </PropertyGroup>

  <ItemGroup>
    <!-- Doench 2016 Rule Set 2 / Azimuth trained models (compact binary blobs; see
         scripts/azimuth/extract_azimuth_model.py for provenance and the binary format). -->
    <EmbeddedResource Include="Resources\azimuth_rs2_nopos.bin" />
    <EmbeddedResource Include="Resources\azimuth_rs2_full.bin" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Seqeron.Genomics.Infrastructure\Seqeron.Genomics.Infrastructure.csproj" />
    <ProjectReference Include="..\Seqeron.Genomics.Core\Seqeron.Genomics.Core.csproj" />
    <ProjectReference Include="..\Seqeron.Genomics.Analysis\Seqeron.Genomics.Analysis.csproj" />
  </ItemGroup>

  <ItemGroup>
    <Using Include="Seqeron.Genomics.Infrastructure" />
    <Using Include="Seqeron.Genomics.Core" />
    <Using Include="Seqeron.Genomics.Analysis" />
  </ItemGroup>

</Project>
