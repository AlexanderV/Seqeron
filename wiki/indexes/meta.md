# Meta indexes

Wiki maintenance, coverage, and ingestion-backlog pages.

Return to the [[index|Wiki Index]].

- [[backlog]] — active ingestion backlog: `docs/algorithms/**` coverage reconciliation (227 covered-via-concept / 0 pending — the per-domain algorithm-doc backlog is closed) plus the source batches queued for ingestion (10 checklists, 4 Validation ledgers, 3 MCP top-level docs).
- [[backlog-covered]] — the chronological resolution-note history and the *Covered via concept (done)* table split out of [[backlog]] (2026-07-18) to keep the active hub under the size cap.
- [[backlog-pending]] — the per-domain pending algorithm-doc tables split out of [[backlog]] (168 docs, 27 domains) to keep the hub under the size cap.
