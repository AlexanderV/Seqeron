# Synthesis

Cross-cutting analyses and compiled indexes.

Return to the [[index|Wiki Index]].

- [[evidence-artifact-index]] — flat per-unit index of the 190 `docs/Evidence/**` source-summary pages, split out of the [[algorithm-validation-evidence]] concept.
- [[comparative-genomics-pipeline-silent-traps]] — the five silent semantic traps in an OLC → ANI → RBH → variant → p-distance pipeline, each with its workaround, plus the assembly-error cascade.
