{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "toolName": "vcf_is_het",
  "serverName": "Parsers",
  "version": "1.0.0",
  "stability": "stable",
  "methodId": "VcfParser.IsHet",
  "docRef": "Seqeron.Genomics/VcfParser.cs#L554",
  "description": "Check if a genotype is heterozygous (different alleles).",
  "inputSchema": {
    "type": "object",
    "properties": {
      "genotype": { "type": "string", "description": "Genotype string (e.g., '0/0', '0/1', '1/1')", "minLength": 1 }
    },
    "required": ["genotype"]
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "result": { "type": "boolean", "description": "True if heterozygous" },
      "genotype": { "type": "string", "description": "Input genotype string" },
      "checkType": { "type": "string", "description": "Check type identifier" }
    },
    "required": ["result", "genotype", "checkType"]
  },
  "examples": [
    { "name": "Heterozygous", "input": { "genotype": "0/1" }, "output": { "result": true, "genotype": "0/1", "checkType": "Heterozygous" } },
    { "name": "Homozygous", "input": { "genotype": "1/1" }, "output": { "result": false, "genotype": "1/1", "checkType": "Heterozygous" } }
  ]
}
