# Findings Register — full disposition of every note across the validation campaign

**Date:** 2026-06-12   **Library:** Seqeron.Genomics (mission-critical)
**Source:** every note/limitation/follow-up across all 86 reports in `docs/Validation/reports/`.

Each finding is placed in exactly one category:

| Category | Meaning |
|----------|---------|
| **FIXED-NOW** | Non-radical (doc/comment/spec text or small safe code change). Fixed in this pass. |
| **FEASIBLE → IMPLEMENT** | Needs real code change but is implementable safely with strict tests, in its own context. |
| **NOT-POSSIBLE (radical)** | Requires a redesign / public-API change / new model. Documented here, not changed. |
| **BY-DESIGN** | Note documents *correct, sourced* intended behaviour; "fixing" it would be wrong. No change. |

---

## A. FIXED-NOW — non-radical (doc/comment/spec) + small safe code fixes

| # | Unit | Finding | Fix |
|---|------|---------|-----|
| A1 | DISORDER-PRED-001 | Stale residue-ranking string `…Q,K,S,E,P` in Evidence doc & TestSpec (S<K, so order is `Q,S,K,E,P`). Code comment already fixed mid-campaign. | Correct the string in Evidence + TestSpec. |
| A2 | MIRNA-PRECURSOR-001 | Evidence "Dataset 3": hsa-mir-21 "8 consecutive pairs" → actually 16. | Correct Evidence text. |
| A3 | MIRNA-PRECURSOR-001 | INV-9 "all else equal" wording imprecise vs M11 (varies stem+loop). | Tighten spec wording. |
| A4 | MIRNA-PRECURSOR-001 | Stale checklist method names `FindPreMiRnas`/`ValidateHairpin` → real `FindPreMiRnaHairpins`. | Fix `ALGORITHMS_CHECKLIST_V2.md`. |
| A5 | SEQ-GCSKEW-001 | Stale method name `FindOriginOfReplication` → `PredictReplicationOrigin` in checklist line ~1543. | Fix `ALGORITHMS_CHECKLIST_V2.md`. |
| A6 | CHROM-KARYO-001 | `GetChromosomeBaseName` comment mentions "chr1a, chr1b" but code handles only `_N` integer suffixes. | Correct the code comment. |
| A7 | PROBE-DESIGN-001 | Stale inline comment "50-70 bp" in M11 test (params are 50-60); assertion already uses live params. | Correct the test comment. |
| A8 | PHYLO-DIST-001 | Evidence §5.3 prose rounds pure-transversion K2P to 0.31726; exact is 0.317162 (test tolerance fine). | Correct Evidence prose. |
| A9 | CRISPR-PAM-001 | Reverse-strand `PamSite.TargetStart` is a revComp-string index while `Position` is forward — internally consistent but easy to misread. | Add an XML `<remarks>` caveat on the field (no behaviour change). |
| A10 | META-CLASS-001 | Docs/spec conflate the implemented flat best-hit with Kraken/LCA (overclaim). | De-overclaim the description/XML docs/spec so it honestly states "best-hit, no LCA" (the LCA *implementation* is NOT-POSSIBLE → §C1). |
| A11 | SV-CNV-001 | `LogRatioToCopyNumber` rounded the absolute copy number `round(2·2^log2)` with `MidpointRounding.AwayFromZero`, diverging from the CNVkit reference (`do_call` uses NumPy `ndarray.round()` = round-half-to-even) at every exact half-integer copy number, e.g. copies=0.5→code CN1 vs CNVkit CN0; copies=2.5→3 vs 2. | Switch to `MidpointRounding.ToEven` to match NumPy/CNVkit (also aligns with sibling `CreateSegment`). Strengthened the S1 property test to the exact sourced sequence `[0,1,2,4,8]` (which exposed the bug) and added a half-integer round-half-to-even `[TestCase]`. Full suite 6493 green. |
| A11 | EPIGEN-METHYL-001 (Phase-2) | Test gaps only (no algorithm defect): CHH invalid-third-base branch (`CAN`→null) untested; `FindMethylationSites` positions test used a vacuously-true `All(...)`. | Added `GetMethylationContext_NonAcgtThirdBase_ReturnsNull`; guarded the positions test with `Is.Not.Empty`. Suite 6478 green. |
| A12 | EPIGEN-DMR-001 (Phase-2) | Test-quality only (no algorithm defect): M8 PValue test asserted only `InRange(0,1)` (a deliberately-wrong impl would pass); `FisherExactProbability` error/`n==0`/non-extreme branches and `AnnotateDMRs` null-input were untested. Algorithm itself matches scipy two-sided Fisher exactly across 7 tables and Wikipedia single-table 0.001346076. | Locked M8 + M1 PValues to scipy values (2.475428262210228e-39, 2.070073888186964e-35); added FisherExactProbability negative-cell throw, `n==0`→1.0, symmetric (5,5,5,5)→0.34371820130334063, and AnnotateDMRs null-input tests. Suite 6482 green. |
| A13 | VARIANT-ANNOT-001 (Phase-2) | Test-quality only (no algorithm defect): S1 (ambiguous codon) asserted only `Is.Not.EqualTo(SynonymousVariant)` — a deliberately-wrong impl could pass; public `GetConsequenceRank` had no direct exact-value coverage (only implicit via M13). Algorithm itself matches Constants.pm ranks/impacts and VariationEffect.pm predicates + NCBI transl_table 1 exactly (verified by hand-decode). | Strengthened S1 to assert exact `CodingSequenceVariant`/MODIFIER; added `GetConsequenceRank_MatchesConstantsPmRanks` (exact Constants.pm ranks) and `GetConsequenceRank_OrderingConsistentWithImpactTiers` (INV-5). Suite 6484 green. |
| — | CRISPR-OFF-001, SEQ-ENTROPY-001, PROTMOTIF-FIND-001, KMER-FIND-001 | Backwards comment / rounding typo / stale citation / weak asserts. | **Already fixed mid-campaign** (here for completeness). |
| A14 | SV-DETECT-001 (Phase-2) | **Real algorithm defect.** `IsConcordantOrientation` treated RF (reverse-forward, outward-facing / "everted") pairs as concordant, so an RF pair was never flagged discordant; `ClassifySV` had no Duplication branch. Per DELLY (Rausch et al. 2012), LUMPY, Manta and SVXplorer (Kumar et al. 2020) — and the unit's own cureffi/BWA source ("RF, FF or RR … that's a problem") — RF is the basic **tandem-duplication** signature for a short-insert FR library, hence discordant; only FR is the proper-pair (FLAG 0x02) orientation. Test S4 was green-washed (asserted RF "not discordant"). Description (SV_Detection.md), Evidence and TestSpec all repeated the wrong "FR/RF both concordant" claim. | Fixed `IsConcordantOrientation` → FR only; added RF→`SVType.Duplication` branch to `ClassifySV`; rewrote S4 to assert RF discordant→Duplication; added M9 ClassifySV Duplication test; corrected the description/Evidence/TestSpec and added DELLY + SVXplorer refs. Full suite 6485 green. |
| A15 | TRANS-SPLICE-001 (Phase-2) | **Real algorithm defect.** `DetectAlternativeSplicing` swapped the A5SS/A3SS labels: two alternative exons sharing their START but differing in END were classified `AlternativeThreePrimeSS`, and sharing their END but differing in START as `AlternativeFivePrimeSS` — the reverse of the standard convention. On the + strand the 5′ splice site = donor = the 3′ END of the upstream exon and the 3′ splice site = acceptor = the 5′ START of the downstream exon, so shared-start/different-end is an alternative **donor** = **A5SS** and shared-end/different-start is an alternative **acceptor** = **A3SS** (verified vs Xinglab/rmats-turbo README A5SS/A3SS coordinate columns — A5SS forms differ at the downstream/END boundary, A3SS at the upstream/START boundary; corroborated by NAR 34(21):6305 and the molecular-biology donor/acceptor definitions). Tests M8/M9, the TestSpec rows and the Evidence dataset all encoded the same swapped labels (code-echoes that would pass against the wrong code). | Swapped the two branch labels in `ClassifyIsoformPair`; corrected M8/M9 test names + assertions to the sourced types and added Start/End span assertions; corrected the TestSpec M8/M9 rows + §5.6 table and the Evidence dataset table; added the rMATS coordinate-convention evidence point. No other caller depends on the labels. Full suite 6501 green. |

| A17 | CODON-RSCU-001 (Phase-2) | **Test-coverage gap only (no algorithm defect).** `CalculateRscu`/`CountCodons` realise the canonical RSCU family-ratio formula `RSCU_{i,j}=(n_i·x_{i,j})/Σx` verified verbatim this session against the **fetched** LIRMM "RSCU RS" page (`RSCU_{i,j}=(n_i×x_{i,j})/Σ x_{i,j}`, no-bias=1) and GenomicSig (CRAN) (bounds `[0,n_i]`, observed/expected-under-uniform), corroborated by seqinr `uco` (Sharp/Tuohy/Mosurski 1986 primary ref). All values independently hand-computed and confirmed by the code: Leu `CTGCTGCTGCTA` 4.5/1.5/0 (Σ=6); Phe `TTTTTTTTC` 4/3,2/3; equal `TTTTTC` 1.0/1.0; Met `ATGATG` 1.0; absent family `ATGATG`→0; stop family `TAATAGTGA` 1.0 each; biased stop `TAATAATGA` 2.0/1.0/0.0 (Σ=3). The original 16-test fixture covered all formula paths/invariants/CountCodons/guards but left two **documented** Stage-A branches untested. | Added three exact-value tests (independently sourced, not code echoes): `CalculateRscu_AbsentFamily_ReturnsZeroForEveryCodon` (locks Assumption #1: 0/0 family→0, no pseudocount); `CalculateRscu_StopCodonFamily_TreatedAsThreeFoldFamily` and `CalculateRscu_StopFamilyBiased_ComputesFamilyRatioAndSumsToDegeneracy` (lock Assumption #2: TAA/TAG/TGA as a 3-fold family via the same family-ratio formula). No assertion weakened, no skip, no tolerance widened. Fixture 16→19 tests. Full unfiltered suite 6526 green, build 0 errors. |
| A16 | SEQ-MW-001 (Phase-2) | Doc typo + test-coverage gaps, no algorithm defect. (1) Evidence line 104 and TestSpec §1.2/M3 stated RNA "AGC" = 997.6177; the correct value from the cited Biopython `unambiguous_rna_weights` is **997.6077** (347.2212+363.2206+323.1965−2·18.0153) — the code and the M3 test already used 997.6077. (2) The "all-unknown ⇒ 0" branch (code returns 0 when no monomer is recognized, distinct from the null/empty short-circuit; spec §3.3) was untested, and the RNA `U` / DNA `T` table entries were never exercised (AGC omits both). Algorithm itself matches Biopython tables/formula and Expasy exactly; independently cross-checked Met-enkephalin YGGFM avg 573.66 (Wikipedia) and insulin A-chain linear ~2383.7. | Corrected 997.6177→997.6077 in Evidence + TestSpec; added `…_AllUnknownSymbols_ReturnsZero` (protein + nucleotide), `…_DnaACGT_ReturnsExactAverageMass` (1253.8027) and `…_RnaACGU_ReturnsExactAverageMass` (1303.7737), each traced to the Biopython tables. Fixture 16→18 tests. Full suite 6516 green, 0 errors. |

## B. FEASIBLE → IMPLEMENT (own context, strict tests first, project patterns)

| # | Unit | Finding | Planned implementation |
|---|------|---------|------------------------|
| B1 | PROTMOTIF-PROSITE-001 | Extended ScanProsite `*` (Kleene-star) query metachar silently dropped → a pattern using it is silently mis-parsed. | **Reject** unsupported PROSITE metacharacters with a clear exception (mirror the Newick "throw, don't silently drop" fix). Strict tests. |
| B2 | POP-FST-001 | `CalculateFst` silently truncates to `min(count1,count2)` on mismatched locus counts. | Validate equal locus counts → throw `ArgumentException` on mismatch (defensive contract). Strict tests. |
| B3 | PARSE-EMBL-001 | INSDC doubled-quote escaping `""`→`"` not unescaped in qualifier values; plus a dead `QualifierRegex` path. | Unescape `""`→`"` in qualifier parsing; remove dead code. Strict tests with the doubled-quote case. |
| B4 | SPLICE-PREDICT-001 (LIMITED) | Region shorter than `minExonLength` dropped by `DeriveExons` but kept by `GenerateSplicedSequence` → coverage/spliced-length invariants (INV-3/4/11) inconsistent. | Make the two consistent (spliced sequence derives from the same reported-exon set), restoring all invariants while preserving `minExonLength` semantics and the `start<end` property test. Strict tests. |

## C. NOT-POSSIBLE (radical — documented, not changed in this pass)

| # | Unit | Gap | Why radical / what a real fix needs |
|---|------|-----|--------------------------------------|
| C1 | META-CLASS-001 | True taxonomic classification (Kraken weighted root-to-leaf / LCA). | Needs a taxonomy DAG data model, LCA-at-DB-build, per-read classification tree — changes the public API and the semantics of 27 locked tests. (Overclaim text is corrected in A10.) |
| C2 | Phylogenetics (PHYLO-NEWICK / TREE / COMP) | Full N-ary (multifurcating) trees. | `PhyloNode` is a binary `Left`/`Right` model; N-ary requires refactoring the node type and every consumer (UPGMA, NJ, RF, MRCA, Newick). Mitigated: Newick now **throws** instead of silently truncating (done mid-campaign). |
| C3 | PHYLO-COMP-001 | Unrooted-bipartition Robinson-Foulds as an alternative to the current rooted-clade metric. | Different metric; additive feature + new tests. Current rooted-clade RF is correct and documented. |
| C4 | ALIGN-MULTI-001 | Guide-tree progressive MSA. | Current star alignment is honestly declared; progressive MSA is a different algorithm (guide tree + profile-profile). |
| C5 | RESTR-DIGEST-001 | Circular-molecule digest. | Needs a topology parameter + wrap-around fragment joining; current linear digest is correct and documented. |
| C6 | ANNOT-GENE-001 | Reverse-strand ribosome-binding-site (SD) reporting in the RBS helper. | Feature addition (PredictGenes already covers both strands). |
| C7 | CRISPR-GUIDE/OFF-001, PRIMER-TM-001 | Doench/Azimuth on-target, MIT/Hsu off-target weight model, SantaLucia NN Tm. | New scientific models; current methods are honestly-declared heuristics, not defects. |
| C8 | PARSE-EMBL-001 | Remote references, deprecated single-dot ranges, dedicated site-between flag. | Rare/edge INSDC location features; out of standard-record scope. |

## D. BY-DESIGN — correct, sourced behaviour; intentionally unchanged

| Unit | Note | Why no change |
|------|------|---------------|
| PARSE-FASTA-001 | Orphan sequence (no header) / header with no sequence silently skipped. | Documented, matches Biopython lenient convention; "fixing" would break the sourced contract. |
| PARSE-FASTQ-001 | Negative-Q (Solexa) unsupported, clamps to Q0. | Spec scopes Phred+33/+64 only; Solexa obsolete. |
| PAT-EXACT-001 | Empty pattern → all positions `[0..n-1]`. | Defined via formal-language ε-substring convention. |
| SEQ-VALID-001 | Empty sequence → valid (true). | Only choice consistent with Biopython + universal-quantifier semantics. |
| ALIGN-SEMI-001 | Fitting-alignment variant (query fully aligned, reference ends free). | Sourced (Rosalind SIMS); correct semi-global variant. |
| ALIGN-MULTI-001 / RF / heuristics | Star MSA, rooted-clade RF, declared heuristics. | Honestly scoped; correct for their stated contract (see C3-C7 for optional upgrades). |
| RNA-STRUCT-001 | `PredictStructure` greedy dot-bracket vs Nussinov/Zuker DP traceback. | Documented approximation; the DP score values themselves are correct. |
| ASSEMBLY-STATS-001 | No defect. N50/L50/Nx/Lx inclusive boundary (`cumulative*100 >= total*threshold`) is algebraically identical to QUAST `s <= limit`; auN = Σl²/Σl; gaps = 0-based inclusive maximal N-runs. All values re-derived from live QUAST `N50.py` + Wikipedia worked examples (A→70/2, B→50/3) + lh3 auN blog. | Implementation and tests both correct; tests assert exact sourced values (no green-washing). Full suite 6497 green, 0 code/test changes. |
| SV-BREAKPOINT-001 | `FindBreakpoints` uses single-linkage on ADJACENT sorted junctions; INV-03 ("reported position within clusterTolerance of EVERY member") can fail on long chains where reported coord = rounded mean. | Single-linkage matches ClipCrop "sorted and clustered within 5-base differences" (adjacent diffs); reported coord is the explicit ASSUMPTION A1 (mean). INV-03 wording overstates the guarantee but the clustering+support logic is sourced and correct. Added chaining test to lock the actual behaviour. |
| ASSEMBLY-OLC-001 | `AssembleOLC` layout is greedy best-successor chaining (not transitive-reduction + Hamiltonian-path) and consensus is realized as chain concatenation (not per-column majority vote). | Exact OLC layout is NP-complete (Compeau et al. 2011); both simplifications are documented and sourced (Langmead OLC/SCS), and for exact-overlap chains concatenation == majority vote. Overlap detection (`FindOverlap`/`FindAllOverlaps`) is exactly correct — 12-edge GTACGTACGAT graph and all overlap/merge values re-derived by hand and matched. Test gap (MinContigLength discard branch had no test) fixed in session: added M5b with sourced exact values. Suite 6494 green. |
| ASSEMBLY-DBG-001 | `BuildDeBruijnGraph`/`AssembleDeBruijn` correct: (k-1)-mer multigraph, prefix→suffix edges, Hierholzer Eulerian walk, `p₀ + last-char(pᵢ)` spelling. No implementation defect — every published example (AAABBBA, a_long_long_long_time k=5, to_every… k=4, ATGGCGTGCA) re-derived from the **fetched** Langmead PDF + an independent Hierholzer reference and matched verbatim. Walk-selection among multiple Eulerian walks is a deterministic single choice (ASSUMPTION-1), so `to_every…` k=3 recovers the input under this walk (Langmead's printed mis-order is his arbitrary walk; C1 asserts only the branch node, correctly). | Test-coverage gap (3 documented branches untested) fixed in session: added `AssembleDeBruijn_DisconnectedGraph_OneContigPerComponent` (two disjoint reads → 2 contigs, Langmead p.24), `AssembleDeBruijn_MinContigLength_FiltersShortContigs` (length-10 contig dropped at MinContigLength=11), `BuildDeBruijnGraph_NullReads_ProducesEmptyGraph`. All values sourced/hand-derived, not code echoes. Fixture 17→20; full suite 6497 green. |
| TRANS-EXPR-001 | No algorithm defect. `CalculateTPM`/`CalculateFPKM`/`QuantileNormalize` realise the verbatim formulas confirmed by primary sources fetched this session (Zhao/Ye/Stanton 2020 PMC7373998 for TPM/RPKM; Wikipedia/Bolstad 2003 worked example for QN). TPM M1=(125000,125000,750000) Σ=1e6, FPKM=500, QN rank means 2/3/14·3⁻¹/17·3⁻¹ and tie mean 31/6 all re-derived and matched. | Test-quality defects fixed in session: (1) M3 was a code-echoing assertion (only `result[0]==result[1]`) — rewritten to exact sourced 500000 for both, and the TestSpec's wrong M3 value (125000) corrected to 500000; (2) FPKM field populated inside `CalculateTPM` was untested — added test asserting exact 83333.33…/83333.33…/500000 (N=60); (3) INV-05 untied-multiset was untested — added test asserting cols 1,3 = {2,3,14/3,17/3}. No green-washing. Fixture 14→16; full suite 6499 green. |
| TRANS-DIFF-001 | No algorithm defect. `CalculateFoldChange` = log2((m2+1)/(m1+1)) and `FindDifferentiallyExpressed` (Welch unequal-variance t-test, exact Student-t tail via regularized incomplete beta, R-style BH step-up, two-criterion gate) reproduce SciPy 1.13.1 / R p.adjust to ≥1e-9 for every checked value this session: log2(41/11)=1.8981203859807863; {1,2,3}↔{7,8,9} t=7.3484692283495345, df=4, p=0.0018262606682599833; BH(0.001,0.4,0.5,0.9)=(0.004,0.6666667,0.6666667,0.9). | Test-quality defect fixed in session: M5 BH test was a **code-echo** (read production raw p back out and compared adjusted p to an in-test reimplementation fed those same values — would pass against a wrong BH). Rewrote M5 to lock externally-sourced raw (SciPy) AND adjusted (R BH) values for genes A–D at 1e-9; added M5b locking the canonical R p.adjust vector (0.001,0.4,0.5,0.9)→(0.004,2/3,2/3,0.9); added a test for the previously-untested se=0/unequal-means branch (p=0, Assumption 3). No green-washing. Fixture 14→17; full suite 6501 green. |
| COMPGEN-ORTHO-001 | No algorithm defect. `FindOrthologs`/`FindReciprocalBestHits`/`FindParalogs` realise the RBH/in-paralog model verified verbatim this session against the **fetched** OUP full text of Moreno-Hagelsieb & Latimer 2008 (RBH def: "find each other as the best hit in the opposite genome"; "coverage of at least 50%"; sort by bit-score then E-value) + Fitch 1970 (speciation vs duplication, via Wikipedia verbatim quote) + InParanoid/Remm 2001 (two-way-best-match seed). Reciprocity is enforced by intersecting both directional best-hit maps; the historical one-directional defect is corrected. All 5-mer Jaccard values hand-recomputed and matched (identical→1.0, ACGT↔AcgtPlusAA→0.667, ACGT↔Tt→0.0). **Notes (PASS-WITH-NOTES):** (A1) alignment-free Jaccard ranking instead of BLAST bit-score (documented Assumption 1); (A2) `FindParalogs` is the within-genome-mutual-best-hit proxy, does not discriminate in/out-paralogs relative to a speciation node (documented §5.3/§6.2); (A3) for this set metric coverage≥identity always, so the 50% coverage gate is subsumed by the identity gate for short DNA (no input found in a 200k random search passing identity but failing coverage). | Test-quality defects fixed in session: (1) a factually-wrong comment claimed TtBlock/GcBlock "unrelated, Jaccard 0.0" — they share 8/12 5-mers (Jaccard 0.5); corrected with the hand value and why the matching is unaffected; (2) the documented `OrthologPair.Coverage` output was unasserted anywhere — added coverage=1.0 assertions to M1/M6; (3) public `FindReciprocalBestHits` was only tested via delegation — added S5 (equivalence to FindOrthologs, exact pairs) and S6 (null guards); strengthened M2 to assert the kept pair's identity is the 1.0 reciprocal hit. No green-washing; exact sourced values. Fixture 13→15; full suite 6506 green. |
| COMPGEN-REARR-001 | No algorithm defect. `DetectRearrangements` realises the signed-permutation breakpoint model verified verbatim this session against the **fetched+pdftotext-decoded** Hunter College CompBio Lecture 16 (breakpoint def "neither (x,y) nor (−y,−x) in extended β"; worked example α=(−2,−3,+1,+6,−5,−4) → b=6 with (−5,−4) excluded; b(β)=0; d≥b/2) plus Tannier d=n−sim and Bafna–Pevzner transposition-preserves-orientation. **Proved the implementation's `y≠x+1` reduction is mathematically exact**: for identity β both criterion clauses `(x,y)=(i,i+1)` and `(−y,−x)=(i,i+1)` collapse to `y=x+1`. All counts independently hand-computed before running code and matched: M1=6, M3/S2=2, M4=2 ((−2,−1) excluded), C1∈[0,n+1]. **Notes (PASS-WITH-NOTES):** (A1) Inversion-vs-Transposition classification is a documented per-boundary local-sign heuristic with no formal theorem (Evidence Assumption 3 / doc §5.3 "intentionally simplified", scope = Inversion/Transposition only); (A2) the doc equates extended-permutation breakpoint count with Tannier's `n−sim`, which differs by telomere-accounting convention — the unit reports the Hunter quantity `b(α)` that the tests check. | Test-coverage gaps fixed in session: (1) **null genome2** was unvalidated by tests though the code throws — added M9b; (2) the documented **`ClassifyRearrangement` fallback** branch (null/unparsable `TargetPosition` → return stored `Type`) was untested — added M10 covering both null and malformed. MUST cases assert exact sourced/hand-derived counts (M1 locks Hunter's 6); no green-washing, no weakened assertions. Fixture 14→16; full suite 6508 green. |
| COMPGEN-SYNTENY-001 | No algorithm defect. `FindSyntenicBlocks` realises the MCScanX collinearity model: `score = n×50 + (−1)×Σ(|Δpos2|−1)`, report iff `score ≥ 250 AND count ≥ 5`, gap guard `<25`, single-direction chains, inverted iff genome-2 order decreases. All constants and the report rule verified verbatim this session against the Oxford-Academic full text of Wang et al. 2012 (MatchScore 50, GapPenalty −1, NumberofGaps<25, "scores over 250 (i.e. at least 5 collinear gene pairs)") and the wyp1125/MCScanX README defaults. Cross-checks hand-derived: 5-fwd→250 (block), 4→200 (none), 6+1gap→299 (block). **Note (PASS-WITH-NOTES, Stage A):** cited 2012 paper says MAX_GAPS "<25" (repo uses 25) but the *current tool* README default is 20 — repo is faithful to its cited primary; "over 250" boundary resolved to `≥250` per the paper's own "≥5 pairs" equivalence (Assumption #1). | Test-coverage gaps fixed in session: contract validated only null genome1 — added null genome2 (S2b) and null orthologMap (S2c); the direction-consistency flush branch (INV-3) was untested — added S4 (ascend 5 then a decrease → 1 forward block of 5, exact). No green-washing; MUST cases assert exact sourced values (M1 locks the score-250 boundary). Fixture 14→17; full suite 6504 green. |
| PANGEN-CORE-001 | **Algorithm defect (fixed).** Core-membership used `coreThreshold = (int)(N·coreFraction)` (floor) with `occupancy ≥ coreThreshold` in both `ConstructPanGenome` and `GetCoreGeneClusters`. Roary's sourced definition (Page et al. 2015, fetched PMC4817141: "a gene being in **at least 99% of samples**") is fractional — `occupancy/N ≥ coreFraction` — **not** floor(coreFraction·N). They diverge for small N: at N=3, coreFraction=0.99 the floor (=2) wrongly classified a 2/3 = 66.7% cluster as core; Roary requires ≥99% (3/3). Fixed with a shared `IsCoreOccupancy(occ,N,cf)` helper (`occ/N ≥ cf − 1e-9`, ε absorbs the 0.99·100 = 98.999… round-off). Fluidity (Kislyuk 2011 PMC3030549, eq. verbatim) and openness (micropan: α<1 open / α>1 closed) were already correct: φ=10/18 hand-derived, M7 α=0→Open and M8 α=1.98→Closed re-derived in Python independently. **Note (PASS-WITH-NOTES):** `DeterminePanGenomeType` fits α by a single dictionary-order log-log regression (order-dependent), not the permutation-averaged micropan nonlinear fit used by the separate `FitHeapsLaw` — disclosed simplification, not a defect. | Test-quality defects fixed in session: S4 was a **green-washed code-echo** locking the wrong floor behaviour ("2/3 is core, CoreGeneCount=2") — rewrote to the sourced fractional rule (CoreGeneCount=1; the 2/3 cluster is accessory), and added S4b locking the exact 99% float boundary (99/100 core, 98/100 not) via `GetCoreGeneClusters`. Description corrected in all three artifacts (algorithm doc, Evidence, TestSpec: INV-03, key-evidence point, S4 row). No assertion weakened; exact sourced values throughout. Fixture 17→18; full suite 6509 green. |
| QUALITY-STATS-001 | **No algorithm defect.** `CalculateStatistics`/`CalculateQ30Percentage` realise the validated formulas: arithmetic mean of decoded Phred Q (= `samtools stats` "average quality" = Σ baseQ ÷ length, and FastQC per-base mean); population σ ÷N (Newcastle); median middle-odd / mean-of-two-central-even (Math is Fun); inclusive %≥Q20/%≥Q30 (Illumina, Q30=99.9%). All cross-checks recomputed in Python this session and matched: {20,30,40}→mean 30.0, popσ 8.16496580927726, median 30; even {20,30,40,40}→35.0; %≥Q30 66.66666666666667; multi-read 6-score mean 30/66.667; Q-table Q20=99/Q30=99.9/Q40=99.99. INV-04 (CalculateQ30Percentage≡PercentAboveQ30) and INV-05 (encoding invariance, Phred+33 "5?I" ≡ Phred+64 "T^h") confirmed. **Note (PASS-WITH-NOTES, Stage A):** `MeanQuality` is the arithmetic mean of Q — a valid, conventionally-reported metric but a crude read-accuracy indicator because Q is logarithmic; the faithful summary averages error probabilities (gigabaseorgigabyte 2017 "the right way"; USEARCH avgq "average Q is a bad idea") or sums expected errors (provided separately by `CalculateExpectedErrors`). | Doc-only fix: added a sourced limitations paragraph to `Quality_Statistics.md` §6.2 disclosing the mean-of-Q vs error-probability-mean distinction and cross-referencing `CalculateExpectedErrors`. No code/behaviour/expected-value change. The 16 existing tests assert exact sourced values within 1e-10 (no green-washing); full suite 6510 green. |
| PANGEN-CLUSTER-001 | **No algorithm defect.** `ClusterGenes` realises the CD-HIT greedy incremental model verified verbatim this session against the **fetched** CD-HIT Algorithm wiki ("sorts the input sequences from long to short… first sequence… first cluster representative… each query… compared to the representative sequences found before it"; fast mode "grouped into the first representative") and the CD-HIT User's Guide wiki ("global sequence identity… number of identical amino acids in alignment divided by the full length of the shorter sequence"; "-c… default 0.9"; "-G… default 1"). All cross-checks hand-computed before running code and matched: identity ATGCATGC≡1.0, vs ATGCATGG=7/8=0.875, vs ATGCATGCAAAA(prefix/shorter 8)=1.0, vs CGTA=0.0; M6 greedy trace (thr 0.8, stable long→short Q2,R,Q1,Q3) → 2 clusters {Q2,R,Q1},{Q3} with consensus AAAAAAAAAAAA. Shorter-length denominator, inclusive `>=`, longest-is-representative, deterministic stable sort, and the all-pairs mean AverageIdentity all confirmed against code. Documented simplifications (ASM-01 ungapped/no internal indels; ASM-02 homolog not ortholog) are in scope. | **Test-quality defects fixed in session:** (1) `PanGenomeAnalyzerTests.ClusterGenes_CalculatesAverageIdentity` asserted `AverageIdentity Is.GreaterThan(0.9)` for two **identical** sequences whose exact value is **1.0** — a green-washed lower bound on the unit under test; tightened to `Is.EqualTo(1.0).Within(1e-10)`. (2) The multi-pair (cluster size ≥3) AverageIdentity averaging path had no exact assertion (M8 only covered a single 2-member pair) — added `ClusterGenes_ThreeMembers_AverageIdentityIsMeanOfAllPairs` pinning the hand-computed (1.0+0.9+0.9)/3 = 2.8/3. Canonical M1–M9/S1–S6/C1–C2 already use exact sourced values (0.875/1.0/0.0, inclusive 0.875 boundary, shorter-length 1.0 at thr 1.0). No assertion weakened, no skip. Minor non-blocking note: TestSpec M4 row label says "0.9 identity" but the implemented M4 uses the stronger 0.875/0.875 exact-boundary test. Fixture 17→18; full suite 6510 green. |
| SEQ-THERMO-001 | **No algorithm defect.** `CalculateThermodynamics` realises the SantaLucia-unified nearest-neighbor model verified verbatim this session: the `NearestNeighborParams` table equals Biopython `DNA_NN3` (Allawi & SantaLucia 1997) and equals the Wikipedia "SantaLucia 1998 unified" kJ table after ÷4.184 (AA −33.1 kJ→−7.91≈−7.9, CG −44.4→−10.61≈−10.6, GC −41.0→−9.80; ΔS likewise). Two-terminus initiation, salt method 5 `0.368·(N−1)·ln[Na+]`, `ΔG=ΔH−310.15·ΔS/1000`, and `Tm=(1000·ΔH)/(ΔS+1.987·ln(C_T/4))−273.15` all match Biopython `Tm_NN`. **Real Biopython 1.85 executed this session** confirms M1 `Tm_NN('CGTTCC…TTAC')`=60.3209→60.3; an independent Python reimplementation reproduced M2 (−30.0,−84.91,−3.67,−18.6), M3 ΔH/ΔS (−23.6,−71.81), M4 (−18.4,−59.91,0.18,−75.0), S2 high-salt (−11.3), C1 Wallace 12, C2 Marmur-Doty 51.78. **BY-DESIGN note:** non-ACGT bases (`N`, RNA `U`) are silently skipped in the NN loop (`TryGetValue`) while still counted in `N` for the salt term — outside the unit's documented ACGT/DNA scope; Biopython instead throws. No correctness impact for valid input; left as-is and noted. | **Test-quality gaps fixed in session (no defect):** (1) M6 length-1 asserted only ΔH+Tm → now asserts all four fields = 0; (2) null input was untested for both public methods → added `CalculateThermodynamics_NullInput_ReturnsAllZero` and `CalculateMeltingTemperature_NullOrEmptyInput_ReturnsZero` (both guard via `string.IsNullOrEmpty`, verified no NRE). All MUST/SHOULD/COULD expectations trace to sourced/Biopython-executed values, not code echoes; no assertion weakened, no skip. Fixture 12→14; full suite 6521 green. |
| SEQ-DINUC-001 | **No algorithm defect.** `CalculateDinucleotideFrequencies`/`CalculateDinucleotideRatios` realise the Karlin genomic-signature model verified verbatim this session against the **fetched** PMC126251 (odds ratio `r_xy = f_xy/(f_x f_y)`; `f_x` normalized base frequency, `f_xy` dinucleotide frequency "in the leading strand"; `r=1` no-bias; symmetrized `r*` = sequence concatenated with inverted complement) and the Karlin & Burge 1995 thresholds restated in MBE 19(6):964 (under ≤0.78 / over ≥1.23, documentation-only). Mixed normalization confirmed correct: base freqs over N, dinucleotide freqs over N−1 (Karlin convention; N vs N−1 is the documented Gardiner-Garden difference). All values independently recomputed in Python `fractions` and matched: `ATGCGCGT` ρ_GC=ρ_CG=64/21, ρ_AT=32/7, ρ_TG=ρ_GT=32/21, f_GC=f_CG=2/7, f_AT=f_TG=f_GT=1/7 (Σ=1); `AAAA` ρ_AA=1.0; `ATAT` ρ_AT=8/3, ρ_TA=4/3; `AUGCGC` f_AU=1/5, f_GC=2/5; `ATGNCG` (ambiguous) f each 1/3, ρ_AT=25/3, ρ_TG=ρ_CG=25/6. **Note:** the `expected>0 ? … : 0` ratio guard is unreachable defensive code (a dinucleotide present in the output always has both bases present ⇒ expected>0); documented, kept. | **Test-quality defects fixed in session:** (1) S4 (`...AbsentBaseContext...`) comment over-claimed it exercised the "expected=0 ⇒ ρ=0" guard (an unreachable branch) — rewrote the comment to state the real behaviour and strengthened it with exact ρ_TA=4/3 and an `Is.EquivalentTo({AT,TA})` key-set assertion (absent G,C produce no keys). (2) Neither method had an ambiguous-base (non-ACGT) exclusion test — added `CalculateDinucleotideRatios_AmbiguousBase_ExcludedFromCounts` (`ATGNCG` → keys {AT,TG,CG}; ρ 25/3, 25/6, 25/6) and `CalculateDinucleotideFrequencies_AmbiguousBase_ExcludedFromCounts` (each f=1/3, Σ=1, exact key set). All values externally recomputed, not code echoes; no assertion weakened, no skip. Codon-frequency method correctly out of scope (migrated to SEQ-CODON-FREQ-001 on 2026-06-14). Fixture 8→10 tests; full suite 6523 green. |
| SEQ-PI-001 | **No algorithm defect.** `CalculateIsoelectricPoint`/`NetCharge` realise the Henderson–Hasselbalch composition model on the EMBOSS Epk.dat pKa scale, verified verbatim this session against the **fetched** EMBOSS `iep` docs (pI = pH where +/− charges balance; Epk.dat Nterm 8.6, Cterm 3.6, C 8.5, D 3.9, E 4.1, H 6.5, K 10.8, R 12.5, Y 10.1) and the **fetched** Peptides `charge_pI.cpp` (basic `+1/(1+10^(pH−pKa))`, acidic `−1/(1+10^(−(pH−pKa)))`). An independent Python reference built from those sources reproduced the Peptides published worked example **exactly** (charge 3.037398 / 2.914112 / 0.7184524 @ pH 5/7/9) and independently re-derived every asserted pI (A 6.10, D 3.75, K 9.70, DDDD 3.23, KKKK 11.27, all-20 7.36, ref-peptide 9.67, DDDDDDDD 2.96, RRRRRRRR 13.35). | **Test-quality defects fixed in session:** (1) M2 (`...StaysWithinPhBounds`) asserted only `[0,14]` on DDDDDDDD/RRRRRRRR — a weak bounds-only check that passes against any in-range implementation; rewritten to lock the exact sourced values **2.96** and **13.35** (bounds retained as added invariant). (2) The documented "non-ionizable chars (gaps/whitespace/non-standard) ignored, no throw" path (§3.3) was untested — added `..._NonIonizableCharacters_IgnoredEqualsTerminiOnly` asserting `"A B!G"`/`"XZ"` → 6.10. All other cases already assert exact sourced values; no green-washing, no skip. Empty/null → 7.0 is a documented non-sourced input-guard convention. Fixture 12→13 tests; full suite 6517 green. |
| SEQ-SECSTRUCT-001 | **No algorithm defect.** `PredictSecondaryStructure` realises the Chou-Fasman windowed mean-propensity profile: per sliding window (step 1, N→C) the per-component arithmetic mean of Pα/Pβ/Pt over the known residues. The 20-residue `SecondaryStructurePropensity` table was verified **verbatim this session** against the **raw** ravihansa3000/ChouFasman `ChouFasman.py` (one-letter-keyed: A 142/83/66 … V 106/170/50, all ÷100 matching the dictionary exactly) and triangulated by CSB\|SJU (19/20 helix+sheet) and a SlideShare table (7 residues across all 3 columns: E 1.51/0.37/0.74, M 1.45/1.05/0.60, A 1.42/0.83/0.66, V, Y, P, G). Window means hand-computed and matched (AE→1.465/0.60/0.70). Wikipedia confirmed the 4-of-6 / 3-of-5 nucleation rules (the full state machine is documented out-of-scope §5.3). **Note (PASS-WITH-NOTES, Stage A):** Lysine Pα = **1.14** (ref impl `['K',114,…]` + Przytycka) vs **1.16** (CSB\|SJU) is a genuine literature reproduction conflict of the 1978 Annu Rev Biochem table; the two-source-majority 1.14 is adopted and locked by test M4. Default windowSize=7 is an API convenience (nucleation windows are 6/5); minor Pt-column variation across reproductions (Ile 0.47 vs 0.50). | No code/test change required. The 16 existing tests already assert exact externally-sourced values (M1-M4 exact tuples; M4 locks Lys 1.14 NOT 1.16 — a regression to 1.16 fails it) and exact arithmetic means within 1e-10; C1/C2's GreaterThan checks are anchored by exact-mean equalities (additive, not weakening). All six invariants and four edge paths (null/empty/window>len/window<1) covered. Coverage note: 12 residues not individually unit-tested but the full 20-residue table was verified verbatim externally and all formula paths are exercised — not a defect. Full unfiltered suite 6523 green; build 0 errors. |

---

## Status

- A (FIXED-NOW): see per-row commits / this pass.
- B (FEASIBLE): implemented in separate contexts with strict tests — status tracked below.
- C (NOT-POSSIBLE): documented above; carried in the ledger's "Deferred BIG fixes" backlog.
- D (BY-DESIGN): no action.

| Item | State |
|------|-------|
| A1–A10 doc/comment fixes | ✅ DONE — all text/comment-only, 0 behaviour change, suite 4503 green. (A1 TestSpec had no such string — only Evidence; A8 corrected to exact 0.317128.) META-CLASS overclaim de-claimed in code XML docs + spec + Evidence. |
| B1 PROSITE reject `*` | ✅ DONE — throws FormatException on unsupported tokens, +6 tests, suite 4495 green, operators byte-for-byte unchanged |
| B2 FST mismatch throw | ✅ DONE — ArgumentException on mismatched locus counts (PairwiseFst inherits), +3 tests, suite 4498 green |
| B3 EMBL `""` unescape | ✅ DONE — INSDC `""`→`"` in EMBL **and** GenBank (old `.Trim('"')` was doubly wrong), dead QualifierRegex removed, +5 tests, suite 4503 green |
| B4 SPLICE-PREDICT invariants | ✅ DONE — consistent-filter fix, +3 strict tests, suite 4489 green, unit now CLEAN |
